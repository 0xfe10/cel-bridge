name: cel_bridge_tools
description: Private build and verification tools for cel-bridge.
publish_to: none
version: 0.1.0

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  archive: ^4.0.9
  cel_bridge:
    path: ../sdk/dart
  crypto: ^3.0.7
  web: ^1.1.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
